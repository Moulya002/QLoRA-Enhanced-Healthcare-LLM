# Assets

Place screenshots and diagrams referenced by the top-level `README.md` here, e.g.:

- `chat_ui.png` — the Streamlit chat interface
- `wandb_dashboard.png` — Weights & Biases training curves
- `eda_length_distributions.png` — copied from `data/raw/` after running EDA

Embed them in the README with:

```markdown
![Chat UI](assets/chat_ui.png)
```
