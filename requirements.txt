# ============================================================================
# Production dependencies for "Domain-Specific LLM Fine-Tuning using QLoRA".
# Pinned to compatible ranges that work together on Python 3.11 + CUDA 12.x.
#
# NOTE on bitsandbytes:
#   4-bit QLoRA training requires a CUDA-capable GPU. On macOS / CPU-only
#   machines `bitsandbytes` will install but cannot run 4-bit ops. The data,
#   evaluation, API and UI code all run CPU-only; only `src/train.py` needs GPU.
# ============================================================================

# ---- Core deep learning ----
torch==2.3.1
transformers==4.43.3
accelerate==0.33.0
peft==0.12.0
trl==0.9.6
bitsandbytes==0.43.1
datasets==2.20.0
sentencepiece==0.2.0
safetensors==0.4.3

# ---- Data handling / EDA ----
pandas==2.2.2
numpy==1.26.4
matplotlib==3.9.1
seaborn==0.13.2

# ---- Experiment tracking ----
wandb==0.17.5

# ---- Evaluation metrics ----
evaluate==0.4.2
rouge-score==0.1.2
sacrebleu==2.4.2
bert-score==0.3.13
nltk==3.8.1

# ---- Serving / API ----
fastapi==0.111.1
uvicorn[standard]==0.30.3
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
httpx==0.27.0

# ---- Frontend ----
streamlit==1.37.0

# ---- Utilities ----
tqdm==4.66.4
